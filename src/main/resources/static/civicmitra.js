(() => {
  const state = { token: localStorage.getItem('civicmitra.token'), user: JSON.parse(localStorage.getItem('civicmitra.user') || 'null'), bins: ['GREEN', 'BLUE', 'RED'], coordinates: null, lastSubmission: null };
  const el = (id) => document.getElementById(id);
  const toast = el('toast');
  let toastTimer;

  function showToast(message, isError = false) { toast.textContent = message; toast.className = `toast show${isError ? ' error' : ''}`; clearTimeout(toastTimer); toastTimer = setTimeout(() => toast.className = 'toast', 4200); }
  function authHeaders() { return state.token ? { Authorization: `Bearer ${state.token}` } : {}; }
  function updateAuthUI() { const signedIn = Boolean(state.token); el('authState').textContent = signedIn ? `${state.user?.name || state.user?.email || 'Signed in'} · ${state.user?.role || 'account'}` : 'Guest session'; el('loginButton').classList.toggle('hidden', signedIn); el('logoutButton').classList.toggle('hidden', !signedIn); }
  function binInfo(type) { return { GREEN: ['Wet Waste', 'Green bin'], BLUE: ['Dry Waste', 'Blue bin'], RED: ['Sanitary Waste', 'Red bin'], BLACK: ['Hazardous Waste', 'Black bin'] }[type]; }
  function renderBins() { el('binInputs').innerHTML = state.bins.map((type, index) => { const [name, detail] = binInfo(type); return `<div class="bin-input"><span class="bin-color ${type.toLowerCase()}">▣</span><div><strong>${name}</strong><small>${detail}${type === 'BLACK' ? ' · Optional' : ''}</small></div><label class="upload-mini" title="Upload ${name} photo"><span>＋</span><input data-index="${index}" type="file" accept="image/*" ${type === 'BLACK' ? '' : 'required'}></label></div>`; }).join('');
    document.querySelectorAll('.upload-mini input').forEach(input => input.addEventListener('change', () => input.parentElement.classList.toggle('has-file', Boolean(input.files[0])))); }
  function formatDate(value) { if (!value) return '—'; const date = new Date(value); return Number.isNaN(date.valueOf()) ? value : new Intl.DateTimeFormat('en-IN', { dateStyle: 'medium', timeStyle: 'short' }).format(date); }
  function requestLocation(statusElement) { if (state.coordinates) return Promise.resolve(state.coordinates); if (!navigator.geolocation) return Promise.reject(new Error('Location services are unavailable in this browser.')); statusElement.textContent = 'Confirming your location…'; return new Promise((resolve, reject) => navigator.geolocation.getCurrentPosition(position => { state.coordinates = { lat: position.coords.latitude, lng: position.coords.longitude }; statusElement.textContent = 'Location confirmed.'; resolve(state.coordinates); }, () => reject(new Error('Please allow location access to continue.')), { enableHighAccuracy: true, timeout: 10000, maximumAge: 60000 })); }
  async function api(url, options = {}) { const response = await fetch(url, { ...options, headers: { ...authHeaders(), ...(options.headers || {}) } }); const contentType = response.headers.get('content-type') || ''; const data = contentType.includes('application/json') ? await response.json() : await response.text(); if (!response.ok) throw new Error(typeof data === 'string' ? data : data.error || data.message || `Request failed (${response.status})`); return data; }
  function renderAnalysis(data) { el('analysisEmpty').classList.add('hidden'); el('analysisResult').classList.remove('hidden'); el('analysisSubtitle').textContent = data.status === 'APPROVED' ? 'Your bins are ready for collection.' : 'A few things need your attention.'; el('scoreValue').textContent = `${Math.round((data.overallScore || 0) * 100)}%`; el('resultRows').innerHTML = (data.binResults || []).map(bin => `<div class="result-row ${bin.passed ? 'good' : 'bad'}"><strong>${bin.binType} bin</strong><span>${bin.passed ? 'Looks good ✓' : bin.contaminationDetail || 'Needs attention'}</span></div>`).join('') || '<div class="result-row"><strong>Bin check</strong><span>Checking your photos</span></div>'; const message = data.status === 'APPROVED' ? 'Great work. Your pickup pass is ready.' : data.failureReason || 'Please make the suggested changes and try once more.'; el('analysisMessage').className = `notice${data.status === 'APPROVED' ? '' : ' warning'}`; el('analysisMessage').textContent = message; }
  function renderQR(data) { el('submissionId').textContent = data.submissionId ? (data.status === 'APPROVED' ? 'Ready for pickup' : 'Update needed') : 'Not checked yet'; el('submissionTime').textContent = formatDate(data.submittedAt); if (data.status !== 'APPROVED' || !data.qrCodeBase64) { el('qrContent').innerHTML = `<span class="qr-leaf">⌁</span><h2>A quick update needed</h2><p>${data.failureReason || 'Make the suggested changes and check your bins again.'}</p>`; return; } el('qrContent').innerHTML = `<span class="qr-leaf">⌁</span><h2>Your pickup pass is ready</h2><p>Show this code to the collection team at your door.</p><img class="qr-image" alt="Pickup pass" src="data:image/png;base64,${data.qrCodeBase64}"><span class="valid-until">◷ Ready until ${formatDate(data.qrExpiresAt)}</span>`; el('qrToken').value = data.qrToken; }
  async function submitVerification(event) { event.preventDefault(); if (!state.token) { el('loginDialog').showModal(); showToast('Please sign in to check your bins.', true); return; } const householdId = el('householdId').value.trim(); const files = [...document.querySelectorAll('#binInputs input[type=file]')].map(input => input.files[0]); if (!householdId || files.some((file, index) => !file && state.bins[index] !== 'BLACK')) { showToast('Add your home reference and a photo for each required bin.', true); return; } let location; try { location = await requestLocation(el('locationStatus')); } catch (error) { el('locationStatus').textContent = error.message; showToast(error.message, true); return; } const data = new FormData(); data.append('householdId', householdId); data.append('lat', location.lat); data.append('lng', location.lng); files.forEach((file, index) => { if (file) { data.append('binImages', file); data.append('binTypes', state.bins[index]); } }); const button = el('submitButton'); button.disabled = true; button.textContent = 'Checking bins…'; try { const result = await api('/api/v1/segregation/submit', { method: 'POST', body: data }); state.lastSubmission = result; renderAnalysis(result); renderQR(result); showToast(result.status === 'APPROVED' ? 'Your pickup pass is ready.' : 'Your bin check is ready to review.'); } catch (error) { showToast(error.message, true); } finally { button.disabled = false; button.textContent = 'Check my bins'; } }
  async function verifyPickup() { if (!state.token) { el('loginDialog').showModal(); showToast('Please sign in before confirming a pickup.', true); return; } const token = el('qrToken').value.trim(); const workerId = el('workerId').value.trim(); if (!token || !workerId) { showToast('Enter your staff number and the pickup code.', true); return; } let location; try { location = await requestLocation(el('workerLocationStatus')); } catch (error) { el('workerLocationStatus').textContent = error.message; showToast(error.message, true); return; } const params = new URLSearchParams({ tokenId: token, workerId, workerLat: location.lat, workerLng: location.lng }); const button = el('verifyButton'); button.disabled = true; button.textContent = 'Confirming…'; try { const result = await api(`/api/v1/segregation/verify-qr?${params}`, { method: 'POST' }); const valid = result.scanResult === 'VALID'; el('verificationStatus').textContent = valid ? 'Collection confirmed' : 'Collection could not be confirmed'; el('householdSummary').innerHTML = `<span class="avatar">${valid ? '✓' : '!'}</span><div><strong>${result.houseNumber || 'Home collection'}</strong><small>${result.message}</small></div>`; el('scanResult').textContent = valid ? 'Ready' : 'Needs review'; el('collectionState').textContent = result.collected ? 'Collected' : 'Not collected'; el('acceptButton').disabled = !result.collected; el('reportButton').disabled = !result.collected; el('completionContent').innerHTML = valid ? '<span class="completion-mark">✓</span><h2>Pickup completed</h2><p>Collection has been recorded successfully.</p>' : '<span class="completion-mark">!</span><h2>Pickup not recorded</h2><p>Please review the collection details before continuing.</p>'; el('nextStop').textContent = valid ? 'Next stop is ready' : 'Review required'; el('nextStopMeta').textContent = valid ? 'Continue to the next home on your route.' : result.message; showToast(result.message, !valid); } catch (error) { showToast(error.message, true); } finally { button.disabled = false; button.textContent = 'Confirm collection'; } }
  async function loadHistory() { const householdId = el('householdId').value.trim(); if (!state.token) { el('loginDialog').showModal(); showToast('Sign in before loading verification history.', true); return; } if (!householdId) { showToast('Enter the household ID first.', true); return; } const list = el('historyList'); list.innerHTML = '<p>Loading verification history…</p>'; try { const history = await api(`/api/v1/segregation/history/${encodeURIComponent(householdId)}`); list.innerHTML = history.length ? history.map(item => `<article class="history-item"><div><strong>Submission #${item.submissionId}</strong><small>${formatDate(item.submittedAt)}</small></div><div><span class="status-chip ${item.status === 'APPROVED' ? '' : 'failed'}">${item.status.replace('_', ' ')}</span></div><div><strong>${Math.round((item.overallScore || 0) * 100)}%</strong><small>AI verification score</small></div><div><strong>Attempt ${item.attemptNumber}</strong><small>${item.qrExpiresAt ? 'QR issued' : item.failureReason || 'No QR issued'}</small></div></article>`).join('') : '<p>No submissions were found for this household.</p>'; } catch (error) { list.innerHTML = `<p>${error.message}</p>`; showToast(error.message, true); } }
  async function login(event) { event.preventDefault(); const button = el('loginSubmit'); button.disabled = true; el('loginError').textContent = ''; try { const result = await fetch('/api/v1/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: el('loginEmail').value, password: el('loginPassword').value }) }); const data = await result.json(); if (!result.ok) throw new Error(data.error || 'Unable to sign in.'); state.token = data.token; state.user = data; localStorage.setItem('civicmitra.token', data.token); localStorage.setItem('civicmitra.user', JSON.stringify(data)); updateAuthUI(); el('loginDialog').close(); showToast(`Welcome, ${data.name || data.email}.`); } catch (error) { el('loginError').textContent = error.message; } finally { button.disabled = false; } }
  el('submissionForm').addEventListener('submit', submitVerification); el('verifyButton').addEventListener('click', verifyPickup); el('loadHistoryButton').addEventListener('click', loadHistory); el('loginButton').addEventListener('click', () => el('loginDialog').showModal()); el('closeLogin').addEventListener('click', () => el('loginDialog').close()); el('loginForm').addEventListener('submit', login); el('logoutButton').addEventListener('click', () => { localStorage.removeItem('civicmitra.token'); localStorage.removeItem('civicmitra.user'); state.token = null; state.user = null; updateAuthUI(); showToast('You have been signed out.'); }); el('addBinButton').addEventListener('click', () => { if (!state.bins.includes('BLACK')) { state.bins.push('BLACK'); renderBins(); } }); el('reportButton').addEventListener('click', () => showToast('This pickup needs a supervisor review. No collection was recorded.', true)); document.querySelectorAll('[data-scroll]').forEach(button => button.addEventListener('click', () => document.getElementById(button.dataset.scroll)?.scrollIntoView({ behavior: 'smooth', block: 'center' }))); renderBins(); updateAuthUI();
})();
