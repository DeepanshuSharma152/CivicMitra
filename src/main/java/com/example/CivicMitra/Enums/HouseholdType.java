package com.example.CivicMitra.Enums;


public enum HouseholdType {
    RESIDENTIAL,        // normal household — 4 bin system
    BULK_GENERATOR,     // societies, institutions — must process at source
    COMMERCIAL          // shops, restaurants — different rules
}
