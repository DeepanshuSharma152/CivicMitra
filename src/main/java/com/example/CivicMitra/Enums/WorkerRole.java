package com.example.CivicMitra.Enums;

public enum WorkerRole {

    /** Ground-level sanitation worker who scans QR tokens and collects waste. */
    COLLECTOR,

    /** Supervisor overseeing a set of collectors on a route or ward. */
    SUPERVISOR
}
