// Next.js App Router nested route: /authority/reports (authority -> reports -> page.tsx)
import { AuthorityReports } from "@/components/authority-reports";

export default function AuthorityReportsPage() {
  return <AuthorityReports />;
}

